export default function handler(req, res) {
  res.status(200).json({
    status: 'placeholder',
    message: 'Auth endpoint placeholder — integrate Auth0, Clerk, or NextAuth and protect /demo',
  })
}
